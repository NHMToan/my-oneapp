mutation UpdateProfile(
  $updateProfileInput: UpdateProfileInput!
  $avatarFile: Upload!
) {
  updateProfile(
    updateProfileInput: $updateProfileInput
    avatarFile: $avatarFile
  ) {
    code
    success
    message
    profile {
      ...profileInfo
    }
  }
}
